{% extends "admin/layout-adm.njk" %}

{% import "parts/card.njk" as card %}

{% block content %}

{% call card.render('Cadastro de Instituições de Ensino', 'colleges-admin container') %}

<div class="menu-bar">

        <a class="button-action" href="/admin/user/{{ user.id }}" title="Editar Usuário">Cadastrar IES</a>

</div>

<div class="content">

    <table width="100%">
        <thead>
            <tr>
                <th>SIGLA</th>
                <th>Razão Social</th>
                <th>Telefone</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
                <tr>
                    <td>{{user.name}}</td>
                    <td>{{user.birth_date}}</td>
                    <td>{{user.cpf}}</td>
                    <td>{{user.phone1}}</td>
                    <td>
                        <div class="action">
                            <a class="button-action" href="/admin/user/{{ user.id }}" title="Editar Usuário">
                                <img src="/images/edit-24.svg" alt="Editar Usuário">
                            </a>
                        </div>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

</div>


{% endcall %}



{% endblock content %}
<header>
    <div class="container">
        
        <div class="logo">
            <a href="{% if session.userId %}
                        /users
                    {% else %}
                        /admin
                    {% endif %}"
            >
                <img src="/images/logo-pq-5.png" alt="">
            </a>            
        </div>

        <nav class="primary">
            <ul>
                <li class="accounts">
                    <a href="/accounts">
                        <i class="material-icons">account_circle</i>
                        Área Restrita
                    </a>
                    {% if session.userId or session.userIdAdmin %}                    
                        <ul class="submenu">
                            <li>
                                <a href="{% if session.userId %}
                                            /users
                                        {% else %}
                                            /admin
                                        {% endif %}"                                   
                                >Painel de Controle</a>
                            </li>
                            <li>
                                <form action="/session/logout" method="POST">
                                    <button>Sair</button>
                                </form>
                            </li>
                        </ul>
                    {% endif %}
                </li>
            </ul>
        </nav>

    </div>

</header>